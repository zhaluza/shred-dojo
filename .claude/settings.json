{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // \"\"' | { read -r f; [[ \"$f\" == *\".utils.\"* ]] && npm run test 2>&1 || true; }",
            "statusMessage": "Running utils tests..."
          }
        ]
      }
    ]
  }
}
